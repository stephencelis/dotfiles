# * Grep shortcut
alias -g "?"="| grep --color --extended-regexp"

# * Open domains with the browser
for domain (com net org edu) alias -s $domain="open"

# * Open logs with tail
alias -s log="tail -200f"

# * Show all with which
alias which="builtin which -a"

# * Bundler
alias b="bundle exec"
