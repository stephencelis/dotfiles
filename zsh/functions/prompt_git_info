if [ -n "$__CURRENT_GIT_BRANCH" ]; then
        local s="$__CURRENT_GIT_BRANCH"
        case "$__CURRENT_GIT_BRANCH_STATUS" in
                ahead)
                s+="↑"
                ;;
                diverged)
                s+="↕"
                ;;
                behind)
                s+="↓"
                ;;
        esac
        if [ -n "$__CURRENT_GIT_BRANCH_IS_DIRTY" ]; then
                s+="%{${fg[yellow]}%}!%{${reset_color}%}"
        fi

        printf "%s " $s
fi
